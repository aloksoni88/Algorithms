/**
 * 
 */
package com.alok.algoexpert.io.medium;

/**
 * @author Alok Soni
 *
 */
public class SingleCycleCheck {
	public static void main(String[] args) {
		int[] arr = {1, 1, 0, 1, 1};
		boolean hasCycle = hasSingleCycle(arr);
		System.out.println(hasCycle);
	}
	
	public static boolean hasSingleCycle(int[] array) {
		if (array == null || array.length == 0) {
			return false;
		}
		boolean[] tmpArr = new boolean[array.length];
			
		int jumpIndex = 0;
		tmpArr[0] = true;
		do {
			jumpIndex = (jumpIndex + array[jumpIndex]) % array.length;
			if (jumpIndex>= 0) {
				tmpArr[jumpIndex] = true;
			} else {
				jumpIndex = array.length + jumpIndex;
				tmpArr[jumpIndex] = true;
			}	
		}while(jumpIndex !=0);
//		while(jumpIndex != 0) {
//			tmpArr[jumpIndex] = true;
//			jumpIndex = (jumpIndex + array[jumpIndex]) % array.length;
//			if (jumpIndex>= 0) {
//				tmpArr[jumpIndex] = true;
//			} else {
//				jumpIndex = array.length + jumpIndex;
//				tmpArr[jumpIndex] = true;
//			}
//		}
		
		for (int i = 0; i < tmpArr.length; i++) {
			if (!tmpArr[i]) {
				return false;
			}
		}
		return true;
	} 
	
//	public static boolean hasSingleCycle(int[] array) {
//		if (array == null || array.length == 0) {
//			return false;
//		}
//		boolean[] tmpArr = new boolean[array.length];
//		for (int i = 0; i < array.length; i++) {
//			int cycleIndex = (i + array[i]) % array.length;
//			if (cycleIndex>= 0) {
//				tmpArr[cycleIndex] = true;
//			} else {
//				cycleIndex = array.length + cycleIndex;
//				tmpArr[cycleIndex] = true;
//			}
//		}
//		for (int i = 0; i < tmpArr.length; i++) {
//			if (!tmpArr[i]) {
//				return false;
//			}
//		}
//		return true;
//	}
}
